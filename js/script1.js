// When the document is ready
        $(document).ready(function() {
            // Select the element with id "p1" and attach a click event handler
            $("#p1").click(function() {
                // When the element is clicked, show an alert
                alert("You clicked the first paragraph!");
            });
        });